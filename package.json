{
  "name": "github-comment-parsing",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "server": "cd apps/server && bun start",
    "server:dev": "cd apps/server && bun dev"
  },
  "devDependencies": {
    "@types/supertest": "^6.0.3",
    "supertest": "^7.2.2"
  }
}
